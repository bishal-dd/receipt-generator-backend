type Version {
  id: UUID!
  mode: String!
  user_id: UUID!
  use_count: Int!
  created_at: DateTime!
  updated_at: DateTime
  deleted_at: DateTime
}

input CreateVersion {
  mode: String!
  user_id: UUID!
}

input UpdateVersion {
  id: UUID!
  mode: String
  user_id: UUID
  use_count: Int
}
